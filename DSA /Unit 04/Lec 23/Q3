Problem Paragraph

You are a student attending a class on sorting techniques taught by Professor Smith.
Professor Smith explains how to efficiently sort both positive and negative
floating-point values using the merge sort algorithm with a recursive approach.
To understand the concept clearly, you decide to write a program that 
implements recursive merge sort to sort floating-point numbers in ascending order.
The final output must display each number rounded to two decimal places.
Input Format
The first line contains an integer n, representing the number of values to be sorted.
The second line contains n space-separated floating-point numbers.
Output Format
Print a single line starting with
Sorted Array:
Followed by n space-separated floating-point numbers, sorted in ascending order.
Each number must be rounded to two decimal places.
Constraints
1 ≤ n ≤ 15
−100.0 ≤ floating-point numbers ≤ 100.0
Sample Test Case 1
Input
5
3.14 1.1 2.71 0.5 1.618
Output
Sorted Array:
0.50 1.10 1.62 2.71 3.14
Sample Test Case 2
Input
6
-2.5 5.0 0.0 -1.5 2.0 3.5
Output
Sorted Array:
-2.50 -1.50 0.00 2.00 3.50 5.00




C++ Code 

#include <iostream>
#include <iomanip>
using namespace std;

void merge(double a[], int l, int m, int r) {
    int n1 = m - l + 1;
    int n2 = r - m;
    double L[n1], R[n2];
    for(int i = 0; i < n1; i++) L[i] = a[l + i];
    for(int j = 0; j < n2; j++) R[j] = a[m + 1 + j];
    int i = 0, j = 0, k = l;
    while(i < n1 && j < n2) {
        if(L[i] <= R[j]) a[k++] = L[i++];
        else a[k++] = R[j++];
    }
    while(i < n1) a[k++] = L[i++];
    while(j < n2) a[k++] = R[j++];
}

void mergeSort(double a[], int l, int r) {
    if(l < r) {
        int m = (l + r) / 2;
        mergeSort(a, l, m);
        mergeSort(a, m + 1, r);
        merge(a, l, m, r);
    }
}

int main() {
    int n;
    cin >> n;
    double a[n];
    for(int i = 0; i < n; i++) cin >> a[i];
    mergeSort(a, 0, n - 1);
    cout << "Sorted Array:" << endl;
    cout << fixed << setprecision(2);
    for(int i = 0; i < n; i++)
        cout << a[i] << " ";
    return 0;
}
